{
  "name": "bonbon-rewards-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.95"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build": "openapi bundle -o dist && npx redocly build-docs ./dist.yaml --output=index.html",
    "test": "openapi lint"
  }
}
